<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= (typeof title !== 'undefined' && title) ? (title + ' - MLVS') : 'MLVS Dashboard' %></title>
  <link rel="stylesheet" href="/dashboard/css/dashboard.css" />
  <% if (typeof pageContext !== 'undefined' && pageContext === 'home') { %>
    <link rel="stylesheet" href="/home/css/landing.css" />
  <% } %>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <script defer src="/dashboard/js/dashboard.js"></script>
  <% if (typeof pageContext !== 'undefined' && pageContext === 'home') { %>
    <script defer src="/home/js/landing.js"></script>
  <% } %>
</head>
<body class="dash-body">
  <div class="dashboard-shell">
    <%- include('partials/sidebar') %>
    <div class="main-column">
      <%- include('partials/header') %>
      <main class="dash-content">
        <%- body %>
      </main>
    </div>
  </div>
</body>
</html>
