<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= botName %> — Home</title>
  <link rel="stylesheet" href="/style.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="topnav">
    <div class="brand">
      <img src="/logo.png" alt="logo" />
      <span><%= brand.title %></span>
    </div>
    <div class="links">
      <a href="/">Home</a>
      <a href="/dashboard">Dashboard</a>
      <% if (brand.inviteUrl) { %><a href="/invite">Invite</a><% } %>
      <% if (brand.supportUrl) { %><a href="<%= brand.supportUrl %>" target="_blank">Support</a><% } %>
      <a href="/api/status" target="_blank">API</a>
      <a href="<%= brand.githubUrl %>" target="_blank">GitHub</a>
    </div>
  </nav>

  <header class="hero" style="background-image: url('<%= brand.bannerUrl %>')">
    <div class="overlay">
      <div class="hero-inner">
        <img class="hero-logo" src="/logo.png" alt="logo" />
        <h1><%= botName %></h1>
        <p class="subtitle"><%= brand.subtitle %></p>
        <div class="badges">
          <span class="badge <%= status==='Online'?'green':'red' %>">Status: <%= status %></span>
          <span class="badge">Servers: <%= guilds %></span>
          <span class="badge">Users: <%= users %></span>
        </div>
        <div class="cta">
          <a class="btn primary" href="/dashboard">Open Dashboard</a>
          <% if (brand.inviteUrl) { %>
            <a class="btn" href="/invite">Invite Bot</a>
          <% } %>
          <% if (brand.supportUrl) { %>
            <a class="btn" href="<%= brand.supportUrl %>" target="_blank">Join Support</a>
          <% } %>
        </div>
      </div>
    </div>
  </header>

  <section class="features">
    <div class="feature">
      <h3>Live Stats</h3>
      <p>Monitor servers and users in real time via the dashboard.</p>
    </div>
    <div class="feature">
      <h3>Command System</h3>
      <p>Categorized commands (general, fun, util, moderation) with rich embeds.</p>
    </div>
    <div class="feature">
      <h3>Branding</h3>
      <p>Customized logo, banner, and colors out of the box.</p>
    </div>
    <div class="feature">
      <h3>Docker Ready</h3>
      <p>Ship and deploy with confidence using Docker/Dokploy.</p>
    </div>
    <div class="feature">
      <h3>Status API</h3>
      <p>Health endpoint for uptime checks and deployments.</p>
    </div>
    <div class="feature">
      <h3>Logging</h3>
      <p>Readable startup banner and request logs.</p>
    </div>
  </section>

  <footer class="footer">
    <div class="left">© <%= new Date().getFullYear() %> <%= brand.title %></div>
    <div class="right">
      <span>Status:</span>
      <span class="badge <%= status==='Online'?'green':'red' %>"><%= status %></span>
    </div>
  </footer>
</body>
</html>
